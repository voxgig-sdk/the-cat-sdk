<?php
declare(strict_types=1);

// TheCat SDK utility: feature_hook

class TheCatFeatureHook
{
    public static function call(TheCatContext $ctx, string $name): void
    {
        if (!$ctx->client) {
            return;
        }
        $features = $ctx->client->features ?? null;
        if (!$features) {
            return;
        }
        foreach ($features as $f) {
            if (method_exists($f, $name)) {
                $f->$name($ctx);
            }
        }
    }
}
